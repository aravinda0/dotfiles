
# Python byte compiled/optimized files
__pycache__/
*.py[cod]

# C extensions
*.so

# Build directory
/build

# Ignore vim's history file used for cross-network edits
/dotfiles/nvim/.netrwhist

# Vim plugins directory
/dotfiles/nvim/plugged

# plug.vim is symlinked into this location during vim_plug installation. We
# don't want to track it.
/dotfiles/nvim/autoload/plug.vim

# Vim persistent undo directory
/dotfiles/nvim/.undo

# Dedicated venv directory for neovim
/dotfiles/nvim/venvs

# Temporary while we have just neovim specific global lua config
.luarc.json
